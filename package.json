{
  "name": "birge-explain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "demo": "node --env-file-if-exists=.env src/cli.js examples/request.json",
    "test": "node --test",
    "ai:prepare": "node --env-file-if-exists=.env src/prepare.js",
    "ai:verify": "node --env-file-if-exists=.env scripts/verify-ai.js",
    "audit": "node --env-file-if-exists=.env scripts/audit.js",
    "test:ui": "playwright test",
    "assets:build": "node scripts/build-hero.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0"
  }
}
