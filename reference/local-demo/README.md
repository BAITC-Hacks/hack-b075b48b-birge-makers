# Archived local demo

These files preserve the original 66-profile browser demo for reference. They are not loaded by index.html or served by the frontend server. The current website uses /api/catalog/options and /api/recommend. Do not use this archive as a fallback when the backend is unavailable.
